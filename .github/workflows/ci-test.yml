name: CI Tests

on:
  pull_request:
    branches:
      - development
      - patch
      - qa
      - main
  push:
    branches:
      - development
      - patch
      - qa
      - main
  workflow_dispatch:

permissions:
  contents: read

jobs:
  tests:
    name: tests
    runs-on: ubuntu-latest
    env:
      PYTHON_VERSION: >-
        ${{
          github.event_name == 'pull_request' &&
          (
            github.base_ref == 'main' && '3.14.2' ||
            github.base_ref == 'patch' && '3.14.2' ||
            github.base_ref == 'qa' && '3.14.3' ||
            '3.14.3'
          ) ||
          (
            github.ref_name == 'main' && '3.14.2' ||
            github.ref_name == 'patch' && '3.14.2' ||
            github.ref_name == 'qa' && '3.14.3' ||
            '3.14.3'
          )
        }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements-dev.txt

      - name: Run tests
        run: pytest
